{
  "name": "surfclub",
  "description": "Surf Club website",
  "version": "0.0.1",
  "license": "MIT",
  "main": "src/index.html",
  "scripts": {
    "start": "parcel src/*.html --open",
    "build": "PARCEL_WORKERS=1 parcel build src/*.html"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.3"
  }
}
